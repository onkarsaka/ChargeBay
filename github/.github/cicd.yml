name : CICD
on:
push:
branches:
-deploy-to-ec2

jobs:
build:
runs-on: ubuntu-latest
-name: Checkout Source
uses: actions/checkout@v3
-name: login to docker hub
run: docker login -u $ {{ secrets.DOCKER_USERNAME }} -p $ {{ secrets.DOCKER_PASSWORD}}
-name: Build Docker Image
run: docker build -t integrationninjas/chargebay .
-name: publish image to docker hub
run: docker push integrationninjas/chargebay:latest

deploy:
needs: build
runs-on: aws-ec2
steps:
-name: Pull image from docker hub
run: docker pull integrationninjas/chargebay:latest
-name: delete old Container
run: docker rm -f reactContainer
-name: run docker container
run: docker run -d -p 3000:80 --name reactContainer integrationninjas/chargebay


